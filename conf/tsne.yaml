defaults:  # inherit parameter values (Hydra inheritance syntax/functionality)
  - common
  - training
# standard value overrides for parameters not used in embedding visualisation script
tokenizer:
  max_seq_len: null
# repurpose output_dir for saving t-SNE plots
output_dir: ???
tsne:
  perplexities:  # list of perplexity values to try for t-SNE
    - 5
    - 30
    - 50

defaults: # inherit parameter values (Hydra inheritance syntax/functionality)
  - common
  - checkpointer
  - data: ???

tokenizer:
  max_seq_len: 2048 # increased max sequence length for SFT (vs CPT)

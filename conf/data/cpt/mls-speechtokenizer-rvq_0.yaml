defaults:
  - _cpt_base

n_dsus: 1024  # SpeechTokenizer RVQ-0 codebook size (Zhang et al., ICLR 2024)

train:
  dataset:
    source: anilkeshwani/mls-speechtokenizer-rvq_0
    interleave_kwargs:
      sampling_rate: ???  # TODO verify — see plans/Bugfix - CPT Interleave Config Sampling Parameters.md
      downsampling_ratio: ???  # TODO verify
dev:
  dataset:
    source: anilkeshwani/mls-speechtokenizer-rvq_0
    interleave_kwargs:
      sampling_rate: ???
      downsampling_ratio: ???

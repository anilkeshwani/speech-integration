defaults:
  - _cpt_base
n_dsus: 8192  # FocalCodec BSQ codebook size, 2^13 (Della Libera et al., arXiv:2502.04465)
train:
  dataset:
    source: anilkeshwani/mls-focalcodec  # TODO confirm HF repo ID
    interleave_kwargs:
      sampling_rate: ???  # TODO verify — see plans/Bugfix - CPT Interleave Config Sampling Parameters.md
      downsampling_ratio: ???  # TODO verify
dev:
  dataset:
    source: anilkeshwani/mls-focalcodec  # TODO confirm HF repo ID
    interleave_kwargs:
      sampling_rate: ???
      downsampling_ratio: ???

defaults:
  - _cpt_base

train:
  dataset:
    source: anilkeshwani/mls-focalcodec  # TODO confirm HF repo ID
    interleave_kwargs:
      sampling_rate: 16000  # TODO confirm FocalCodec model sampling rate
dev:
  dataset:
    source: anilkeshwani/mls-focalcodec  # TODO confirm HF repo ID
    interleave_kwargs:
      sampling_rate: 16000
